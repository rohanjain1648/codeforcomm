# Optional — enables the district hotspot map on the Dashboard.
# Get a key at https://console.cloud.google.com/google/maps-apis
# with "Maps JavaScript API" enabled. Without it, the dashboard shows a
# legend-only placeholder instead of the map.
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
